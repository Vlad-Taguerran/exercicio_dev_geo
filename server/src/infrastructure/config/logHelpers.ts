export const logSuccess = (msg: string) => console.log(`✅ ${msg}`);
export const logError = (msg: string, err: any) => console.error(`❌ ${msg}`);
export const logWarning = (msg: string) => console.warn(`⚠️ ${msg}`);
export const logInfo = (msg: string) => console.info(`ℹ️ ${msg}`);
